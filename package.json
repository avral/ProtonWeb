{
  "name": "proton-web",
  "private": true,
  "devDependencies": {
    "lerna": "^3.19.0"
  },
  "scripts": {
    "watch": "lerna run watch --parallel --stream",
    "start": "lerna run start --stream"
  },
  "workspaces": [
    "packages/*"
  ]
}
